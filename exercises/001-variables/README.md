# Variables

In Java, variables are mutable by default and must be declared with a specific data type.
Once a variable is declared, you can change its value, but you cannot change its data type.
You can make a variable immutable (a constant) by adding the `final` keyword in front of its declaration.

## Further information

- [Variables (The Java™ Tutorials)](https://docs.oracle.com/javase/tutorial/java/nutsandbolts/variables.html)