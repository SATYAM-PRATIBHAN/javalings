// I AM NOT DONE

public class Operators2 {
    // 1. A user is eligible to register if they are at least 18 years old,
    // OR if they possess a special VIP pass.
    public static boolean isEligibleForRegistration(int age, boolean isVip) {
        // Your code here
        return false;
    }

    // 2. A 4-digit PIN is valid only if it is between 1000 and 9999 (inclusive).
    public static boolean isValidPin(int pin) {
        // Your code here
        return false;
    }

    // 3. A user can access the secret area only if ALL the following are true:
    // - They are a VIP
    // - Their age is 21 or older
    // - Their PIN is exactly 9999
    public static boolean canAccessSecretArea(int age, boolean isVip, int pin) {
        // Your code here
        return false;
    }
}
