// I AM NOT DONE

public class Operators1 {
    // 1. Calculate the total cost of purchasing multiple items.
    // The taxRate is given as a whole number (e.g., 5 means 5%).
    // Use arithmetic operators (*, /, +) to calculate the total.
    // For simplicity, we are working with integers, so perform division last to avoid truncating early.
    public static int calculateTotalWithTax(int basePrice, int quantity, int taxRate) {
        // Your code here
        return 0;
    }

    // 2. Calculate how many full items you can buy with a given budget.
    public static int itemsPurchasable(int budget, int itemCost) {
        // Your code here
        return 0;
    }

    // 3. Calculate how much money is left over after buying as many items as possible.
    // Hint: Use the modulo operator.
    public static int remainingBudget(int budget, int itemCost) {
        // Your code here
        return 0;
    }
}
