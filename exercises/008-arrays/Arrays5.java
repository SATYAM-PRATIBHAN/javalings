// I AM NOT DONE
import java.util.Arrays;

public class Arrays5 {
    // 1. Do two arrays contain the exact same elements, even if they are in different orders?
    // Use `Arrays.sort()` to sort both arrays, and then use `Arrays.equals()` to compare them.
    // Note: Arrays.sort() modifies the array in place! You might want to copy them first 
    // using `Arrays.copyOf()` if you were building a real library, but for this exercise, 
    // sorting the originals is fine.
    public static boolean haveSameElements(int[] array1, int[] array2) {
        // Your code here
        return false;
    }
}
