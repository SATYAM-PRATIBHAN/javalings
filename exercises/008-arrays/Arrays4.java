// I AM NOT DONE

public class Arrays4 {
    // 1. Arrays in Java have a fixed size. To add an element, 
    // you must create a new array that is 1 larger than the old one,
    // copy all the old elements over, and place the new element at the end.
    public static int[] appendElement(int[] original, int newElement) {
        // Your code here
        return new int[]{};
    }

    // 2. Remove the element at the given index.
    // Create a new array that is 1 smaller, and copy everything EXCEPT
    // the element at 'indexToRemove'.
    public static int[] removeElementAt(int[] original, int indexToRemove) {
        // Your code here
        return new int[]{};
    }
}
