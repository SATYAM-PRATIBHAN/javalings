// I AM NOT DONE

public class Enums1 {

    // 1. Create an enum named 'TrafficLight'.
    // It should have three constants: RED, YELLOW, and GREEN.
    enum TrafficLight {
        // YOUR CODE HERE
    }

    // 2. Write a method 'getNextLight' that takes a TrafficLight.
    // If it's RED, return GREEN.
    // If it's GREEN, return YELLOW.
    // If it's YELLOW, return RED.
    // Use a switch statement!
    public static TrafficLight getNextLight(TrafficLight currentLight) {
        // YOUR CODE HERE
        return null;
    }
}
