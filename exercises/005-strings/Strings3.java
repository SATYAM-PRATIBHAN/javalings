// I AM NOT DONE

public class Strings3 {
    // 1. You need to generate a precise receipt header.
    // Use String.format() to return a string exactly like this:
    // "Receipt #005 | Total: $45.50"
    // The receipt number should always be at least 3 digits (padded with zeros).
    // The total should always have 2 decimal places.
    public static String generateReceipt(int receiptNumber, double total) {
        // Your code here
        return "";
    }

    // 2. You need to reverse a string for a cryptographic puzzle.
    // Since Strings are immutable, concatenating in a loop is slow.
    // Create a StringBuilder, put the text in it, reverse it, and return as a String.
    public static String reverseText(String text) {
        // Your code here
        return "";
    }
}
