// I AM NOT DONE

public class Strings2 {
    // 1. You receive log lines in this format: "ERROR: Something went wrong!"
    // Extract and return just the log level (e.g., "ERROR").
    public static String extractLogLevel(String logLine) {
        // Your code here
        return "";
    }

    // 2. You receive log lines with a user ID in brackets: "User logged in [405]"
    // Extract the number inside the brackets, convert it to an int, and return it.
    public static int extractUserId(String logLine) {
        // Your code here
        return 0;
    }
}
