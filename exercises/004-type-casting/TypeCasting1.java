// I AM NOT DONE

public class TypeCasting1 {
    // 1. You have a high-precision temperature reading as a double.
    // The display screen can only show whole numbers.
    // Cast the double to an int (this will truncate the decimals) and return it.
    public static int displayTemperature(double rawReading) {
        // Your code here
        return 0;
    }

    // 2. You have a total distance in meters (int) and time in seconds (int).
    // To get an accurate speed (m/s) with decimals, you must prevent integer division.
    // Cast one or both of the variables to a double before dividing.
    public static double calculateExactSpeed(int distance, int time) {
        // Your code here
        return 0.0;
    }

    // 3. Characters in Java are actually numeric values underneath (UTF-16).
    // Cast the given character to its integer representation and return it.
    public static int getAsciiValue(char character) {
        // Your code here
        return 0;
    }
}
