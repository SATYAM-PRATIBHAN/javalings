// I AM NOT DONE

public class Constructors1 {
    static class UserProfile {
        String username;
        int level;

        // 1. Complete the 2-argument constructor taking (String username, int level).
        public UserProfile(String username, int level) {
            // YOUR CODE HERE
        }

        // 2. Complete the 1-argument constructor taking (String username).
        // It MUST use `this(...)` to call the 2-argument constructor, setting level to 1.
        public UserProfile(String username) {
            // YOUR CODE HERE
        }

        // 3. Complete the no-argument constructor.
        // It MUST use `this(...)` to call the 1-argument constructor, setting username to "Guest".
        public UserProfile() {
            // YOUR CODE HERE
        }
    }
}
