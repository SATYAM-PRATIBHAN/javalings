// I AM NOT DONE

public class Loops2 {
    // 1. Simulate a bouncing ball.
    // The ball is dropped from an initial height. 
    // After each bounce, its new height is (currentHeight * bounceFactor).
    // The ball will ALWAYS bounce at least once when dropped.
    // Count and return the number of bounces until the height drops below 1.0.
    // Use a do-while loop!
    public static int countBounces(double initialHeight, double bounceFactor) {
        // Your code here
        return 0;
    }

    // 2. Calculate the factorial of a positive integer 'n' (n!).
    // E.g., 4! = 4 * 3 * 2 * 1 = 24.
    // Use a do-while loop to multiply and decrement 'n'.
    // Assume n >= 1.
    public static int factorial(int n) {
        // Your code here
        return 1;
    }
}
