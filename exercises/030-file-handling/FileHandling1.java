import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Path;

// I AM NOT DONE

public class FileHandling1 {

    // 1. Write a method 'writeToFile' that writes the 'content' String to the given 'path'.
    // Use the Files.writeString() method.
    // Notice that it throws IOException, so you must declare that in the method signature!
    public static void writeToFile(Path path, String content) {
        // YOUR CODE HERE
    }

    // 2. Write a method 'readFromFile' that reads all text from the given 'path' and returns it.
    // Use the Files.readString() method.
    // Declare that it throws IOException!
    public static String readFromFile(Path path) {
        // YOUR CODE HERE
        return null;
    }
}
