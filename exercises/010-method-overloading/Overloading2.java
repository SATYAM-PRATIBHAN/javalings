// I AM NOT DONE

public class Overloading2 {
    // 1. Write a set of overloaded methods named 'process' that handle different data types.
    // - Returns the number multiplied by 10.
    public static int process(int number) {
        return 0; // Fix this
    }

    // Now, write two more overloaded 'process' methods yourself:
    // - process(String text): Returns the text converted to uppercase (as a String).
    // - process(int[] numbers): Returns the sum of the array elements (as an int).
    //
    // YOUR CODE HERE

    
    // 2. What happens if you pass a double to a method expecting an int?
    // Write a method 'process(double number)' that returns the double rounded
    // to the nearest whole number (cast to a long). Use Math.round().
    //
    // YOUR CODE HERE

}
