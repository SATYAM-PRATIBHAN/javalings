// I AM NOT DONE

public class Overloading1 {
    // Method overloading allows you to have multiple methods with the same name,
    // as long as their parameter lists are different (different types or counts).
    //
    // 1. Returns the area of a square (side * side) as an int.
    public static int calculateArea(int side) {
        return 0; // Fix this
    }

    // 2. Returns the area of a rectangle as an int.
    public static int calculateArea(int length, int width) {
        return 0; // Fix this
    }

    // 3. Returns the area of a circle (Math.PI * radius * radius) as a double.
    public static double calculateArea(double radius) {
        return 0.0; // Fix this
    }

}
