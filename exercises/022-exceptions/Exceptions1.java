// I AM NOT DONE

public class Exceptions1 {

    // 1. Complete the 'parseIntOrDefault' method.
    // It should attempt to parse 'input' into an integer using Integer.parseInt(input).
    // If it succeeds, return the parsed integer.
    // If it throws a 'NumberFormatException', catch it and return the 'defaultValue'.
    public static int parseIntOrDefault(String input, int defaultValue) {
        // YOUR CODE HERE
        return 0;
    }
}
